{
  "name": "sc-portfolio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --host 127.0.0.1",
    "build": "npm run build:demos && astro build",
    "preview": "astro preview --host 127.0.0.1",
    "check": "astro check",
    "build:demos": "node scripts/prepare-demos.mjs && npm ci --prefix demos/catatstock && npm run build --prefix demos/catatstock -- --base=/demos/catatstock/ --outDir=../../public/demos/catatstock"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "0.10.22-rc.20250304",
    "astro": "^7.3.3",
    "onnxruntime-web": "1.22.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "typescript": "^5.9.2"
  }
}
